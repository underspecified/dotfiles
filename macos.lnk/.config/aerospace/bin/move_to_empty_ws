#!/usr/bin/env bash
set -euo pipefail

move_to_empty_ws() {
    local empty
    empty="$(aerospace list-workspaces --monitor mouse --empty | head -1)"
    aerospace move-node-to-workspace "${empty}"
}

move_to_empty_ws
