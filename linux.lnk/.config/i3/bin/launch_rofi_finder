#!/bin/bash

CURDIR=$(dirname $0)
DOTFILES=$(realpath $CURDIR/../../..)

mode=$(darkman get)
theme="$DOTFILES/config/rofi/selenized-$mode.rasi"

xdg-open "$(locate -i / | rofi -threads 0 -width 100 -dmenu -i -p "Find" -theme $theme)"
