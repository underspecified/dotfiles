# Agent socket is set by keychain in zshrc.linux (SSH_AUTH_SOCK).
# No IdentityAgent override here — ssh uses SSH_AUTH_SOCK from the env.
#
# github.com + fleet identity (IdentityFile hri_jp) is NOT here — it lives in the
# 1P-pulled ~/.ssh/config.d/hri_jp (setup_ssh.sh hri-jp), shared with macOS.
