# Defensive: ensure nouveau never loads alongside the NVIDIA proprietary
# driver. On this host nouveau is not currently loaded, but pin that state.
blacklist nouveau
blacklist lbm-nouveau
options nouveau modeset=0
alias nouveau off
alias lbm-nouveau off
